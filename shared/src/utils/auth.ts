// import { Role, User } from '../types';

// export const restrictTo = (roles: Role[]) => (user: User) => {
//   if (!user) {
//     throw new Error('Not authenticated');
//   }
//   // if (!roles.includes(user.role)) {
//   //   throw new Error('Not authorized');
//   // }
// };